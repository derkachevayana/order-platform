replicaCount: 1

image:
  repository: ghcr.io/derkachevayana/auth-service
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 8081

probes:
  path: /actuator/health
  port: 8081