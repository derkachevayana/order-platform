replicaCount: 1

image:
  repository: ghcr.io/derkachevayana/user-service
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 8082

probes:
  path: /actuator/health
  port: 8082